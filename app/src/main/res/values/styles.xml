<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HelloDarknessButton" parent="@android:style/Widget.Button">
        <item name="android:background">@color/primaryText</item>
        <item name="android:textColor">@color/primaryBackground</item>
    </style>

    <style name="HelloDarknessEditText" parent="@android:style/Widget.EditText">
        <item name="android:background">@color/primaryText</item>
        <item name="android:textColor">@color/primaryBackground</item>
        <item name="android:editTextBackground">@color/primaryText</item>
        <item name="android:editTextColor">@color/primaryText</item>
    </style>

    <style name="HelloDarkness" parent="AppTheme">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:typeface">normal</item>
        <item name="android:background">@color/primaryBackground</item>

        <item name="android:buttonStyle">@style/HelloDarknessButton</item>
        <item name="android:editTextStyle">@style/HelloDarknessEditText</item>
    </style>

</resources>
