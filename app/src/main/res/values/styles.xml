<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light"></style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HelloDarknessButton" parent="@android:style/Widget.Button">
        <item name="android:background">@color/primaryText</item>
        <item name="android:textColor">@color/primaryBackground</item>
    </style>

    <style name="HelloDarknessEditText" parent="@android:style/Widget.EditText">
        <item name="android:background">@color/primaryText</item>
        <item name="android:textColor">@color/primaryBackground</item>
    </style>

    <style name="HelloDarkness" parent="AppTheme">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:typeface">normal</item>
        <item name="android:background">@color/primaryBackground</item>

        <item name="android:buttonStyle">@style/HelloDarknessButton</item>
        <item name="android:editTextStyle">@style/HelloDarknessEditText</item>
    </style>

    <style name="HelloDarkness.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="HelloDarkness.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="HelloDarkness.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- current theme -->
    <style name="AppTheme.Blue" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">#1565C0</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:windowBackground">@color/primary</item>

        <item name="colorControlNormal">@color/iron</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="android:textColorHint">@color/iron</item>

        <item name="colorButtonNormal">@color/primary_darker_blue</item>
        <item name="android:colorButtonNormal">@color/primary_darker_blue</item>
    </style>

    <style name="AppTheme.Dark.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/iron</item>
        <item name="android:background">@color/primary</item>
    </style>

</resources>
